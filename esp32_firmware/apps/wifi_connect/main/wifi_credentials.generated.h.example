/* Example shape only — real file is generated and gitignored.
 * Run: ./scripts/gen-wifi-credentials.sh
 */
#pragma once
#include <stddef.h>

typedef struct {
    const char *ssid;
    const char *password;
} wifi_known_network_t;

#define WIFI_KNOWN_NETWORK_COUNT 0

static const wifi_known_network_t WIFI_KNOWN_NETWORKS[] = {
};
